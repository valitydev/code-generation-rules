# Protobuf

Use versioned packages and directories. Keep wire-compatible field types, reserve removed fields, and add compatibility tests when a consumer may receive a newly added oneof variant or enum value.
